name: Release

on:
  push:
    tags:
      - v*

env:
  PYTHONUNBUFFERED: "1"
  FORCE_COLOR: "1"

permissions:
  contents: read

jobs:
  pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    environment:
      name: release
    permissions:
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          enable-cache: true
          python-version: "3.11"

      - name: Build package
        run: uv build

      - name: Run smoke test (wheel)
        run: uv run --isolated --no-project -p 3.11 --with dist/*.whl tests/release_smoke_test.py

      - name: Run smoke test (source distribution)
        run: uv run --isolated --no-project -p 3.11 --with dist/*.tar.gz tests/release_smoke_test.py

      - name: Publish package to PyPI
        run: uv publish --trusted-publishing always
